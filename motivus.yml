---
version: 1.0

build:
  compiler: mvrc

package:
  name: {{project-name}}
  version: 0.1.0
  metadata:
    short_description: {{description}}
    license: "{{license}}"
    author: "{{authors}}"
    url: "{{url}}"
    upstream_url: "{{upstream_url}}"
    long_description: ./USAGE.md
